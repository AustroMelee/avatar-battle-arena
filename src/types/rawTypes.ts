// Types matching the exact structure of raw JSON files in /raw-data/*.json

export type RawBending = {
  name: string;
  description: string;
};

export type RawCharacter = {
  name: string;
  description: string;
};

export type RawFauna = {
  name: string;
  description: string;
};

export type RawFood = {
  name: string;
  description: string;
};

export type RawLocation = {
  name: string;
  description: string;
};

export type RawSpiritWorld = {
  name: string;
  description: string;
}; 